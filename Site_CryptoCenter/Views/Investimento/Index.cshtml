@model IEnumerable<Domain_CryptoCenter.Domain.Investimento>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.QuantidadeDisponivel)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Valor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.QuantidadeVendida)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.QuantidadeDisponivel)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Valor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.QuantidadeVendida)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.InvestimentoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.InvestimentoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.InvestimentoId })
        </td>
    </tr>
}

</table>
